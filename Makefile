# ----------------------------------------------------------------------------
# Submodule Dependency Initialization
# ----------------------------------------------------------------------------
init-deps:
	git submodule update --init --recursive

